import React from 'react';
import ReactDOM from 'react-dom/client';
import InvestmentDemo from './InvestmentDemo';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <InvestmentDemo />
  </React.StrictMode>
);